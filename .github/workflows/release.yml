# .github/workflows/release.yml

name: Publish Extension to Marketplace (DECOMMISSIONED)

on:
  workflow_dispatch:

jobs:
  decommissioned:
    runs-on: ubuntu-latest
    steps:
      - name: Extension publishing decommissioned
        run: |
          echo "The VS Code extension has been decommissioned."
          echo "This workflow no longer publishes to the marketplace."
          exit 1
